<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.scau.buymesth">

    <!--允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--获取GSM（2g）、WCDMA（联通3g）等网络状态的信息  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--获取sd卡写的权限，用于文件上传和下载-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许读取手机状态 用于创建BmobInstallation-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="Bmob_APP_KEY"
            android:value="211614edac96ab4c9492179cf459993a" />

        <activity android:name=".ui.WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|stateUnchanged">
        </activity>

        <activity android:name=".ui.RegisterActivity"
            android:theme="@style/AppTheme.SwipeBackLayout"
            android:windowSoftInputMode="stateHidden|stateUnchanged">
        </activity>

        <activity android:name=".main.TabActivity"
            android:theme="@style/AppTheme.SwipeBackLayout">
        </activity>
<!--<activity android:name=".homedetail.HomeDetailActivity" android:theme="@style/AppTheme.SwipeBackLayout">  </activity>-->

        <!--<activity android:name=".home.AddActivity" android:theme="@style/AppTheme.SwipeBackLayout">-->
        <!--<activity android:name=".publish.PublishActivity" android:theme="@style/AppTheme.SwipeBackLayout">-->


        <!--</activity>-->
<activity android:name=".homedetail.DetailActivity" android:theme="@style/BaseTheme"
    android:parentActivityName=".main.TabActivity">
    <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".main.TabActivity"/>
</activity>

<activity android:name=".publish.PublishActivity" android:theme="@style/BaseTheme"/>
    </application>

</manifest>